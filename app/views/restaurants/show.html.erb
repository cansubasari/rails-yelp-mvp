<h1 class="text-success">~Reviews~</h1>

<ul class="list-group">
  <% @restaurant.reviews.each do |review| %>
    <li class="list-group-item"><%= review.content %></li>
  <% end %>
</ul>
<br>
<p><%= link_to "Add New Review", new_restaurant_review_path(@restaurant), class: "text-warning" %></p>

<p><%= link_to "Back to The Restaurants List", restaurants_path %></p>
