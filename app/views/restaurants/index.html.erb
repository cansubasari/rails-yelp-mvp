<h1 class="text-primary">🍽️Restaurants List🍽️</h1>

<% @restaurants.each do |restaurant| %>
  <div>
    <h2><%= link_to restaurant.name, restaurant_path(restaurant), class: "text-dark" %></h2>
    <p class= "text-info"><%= restaurant.category.capitalize %> - <%= restaurant.address %> - <%= restaurant.phone_number %> </p>
  </div>
<% end %>

<p><%= link_to "Add New Restaurant", new_restaurant_path, class: "text-warning" %></p>
